env:
  - name: "AIRFLOW__WEBSERVER__EXPOSE_CONFIG"
    value: "true"

dags:
  persistence:
    enabled: true
    storageClassName: 
    size: 1Gi
    accessMode: ReadOnlyMany
    existingClaim: datagov-dags-pvc